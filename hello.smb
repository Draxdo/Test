#program main
#include vector.smb
#include math.smb

fn main() => {
    let x = Vector(10, [10][1, 2, 3, 4]);
    initRand();
    for i from 0 to x.length - 1;
        x.value[i] = randRange(0, 100);
    endfor;
    printIntegerVector(x);
}

