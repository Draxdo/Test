#program main
#include strings.smb

fn main() => {
    let x = String("Hello ");
    let y = String("World!");
    let z = String(" Jimmy!");
    let a = x + y + z;
    a as struct String;
    printf("%s\n", a.value);
}